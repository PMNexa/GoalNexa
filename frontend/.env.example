# Base URL for the backend API.
# In dev, leave unset to use Vite's dev-server proxy (see vite.config.ts) which
# forwards /api -> http://localhost:8000. Set this explicitly to point straight
# at a backend, e.g. when running the frontend against a remote/staging API.
VITE_API_BASE_URL=http://localhost:8000
