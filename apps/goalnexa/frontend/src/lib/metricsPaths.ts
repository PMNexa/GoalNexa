import { MetricsRouter } from "./metricsRouter";

/** Same "one computation, shared by index.ts and routes/*.tsx" rule as `goalsPaths.ts` - just `MetricsRouter.paths`. */
export const METRICS_PATHS = MetricsRouter.paths;
