import { CheckInsRouter } from "./checkInsRouter";

/** Same "one computation, shared by index.ts and routes/*.tsx" rule as `goalsPaths.ts`/`metricsPaths.ts` - just `CheckInsRouter.paths`. */
export const CHECK_INS_PATHS = CheckInsRouter.paths;
